# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/temp?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=DmK#64@22
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server Port
server.port=8080

# Logging
logging.level.org.springframework=INFO
logging.level.com.rspc.timetable=DEBUG
logging.level.org.hibernate.SQL=ERROR

# Jackson Configuration (for proper JSON serialization)
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.serialization.write-dates-as-timestamps=false

logging.level.org.optaplanner=INFO
logging.level.org.optaplanner.core.impl.solver=TRACE
logging.level.org.optaplanner.core.impl.score=TRACE
